# Practica: Nginx local con Docker Compose

Este proyecto levanta un servidor web Nginx local usando Docker Compose.

## Ejecutar

```bash
docker compose up -d
```

Luego abrir:

```text
http://localhost:8080
```

## Detener

```bash
docker compose down
```
